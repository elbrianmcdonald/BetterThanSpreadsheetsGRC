@model CyberRiskApp.ViewModels.RiskAssessmentViewModel
@{
    ViewData["Title"] = "Edit Risk Assessment";
}

<!-- DEBUG: Confirm Edit view is rendering -->
<div class="alert alert-info" style="position: fixed; top: 10px; right: 10px; z-index: 9999; max-width: 300px;">
    <strong>🔧 DEBUG:</strong> This is Edit.cshtml view<br/>
    <small>Assessment ID: @Model.Assessment.Id</small>
</div>

<div class="container-fluid py-4">
    <div class="row justify-content-center">
        <div class="col-md-10">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h2>
                    <i class="fas fa-edit text-primary me-2"></i>
                    Edit Risk Assessment
                    <span class="badge bg-primary ms-2">ID: @Model.Assessment.Id</span>
                </h2>
                <div>
                    <a asp-action="Details" asp-route-id="@Model.Assessment.Id" class="btn btn-outline-info me-2">
                        <i class="fas fa-eye me-1"></i>View Details
                    </a>
                    <a asp-action="Index" class="btn btn-outline-secondary">
                        <i class="fas fa-arrow-left me-1"></i>Back to List
                    </a>
                </div>
            </div>

            <form asp-action="Edit" method="post" class="needs-validation" novalidate>
                <input type="hidden" asp-for="Assessment.Id" />
                <input type="hidden" asp-for="Assessment.AssessmentType" />

                <div class="row">
                    <div class="col-md-8">
                        <div class="card shadow-sm">
                            <div class="card-header bg-light">
                                <h5 class="mb-0"><i class="fas fa-info-circle me-2"></i>Assessment Details</h5>
                            </div>
                            <div class="card-body">
                                <div class="mb-3">
                                    <label asp-for="Assessment.Title" class="form-label">Assessment Title</label>
                                    <input asp-for="Assessment.Title" class="form-control" placeholder="Enter assessment title" required />
                                    <span asp-validation-for="Assessment.Title" class="text-danger"></span>
                                </div>

                                <div class="mb-3">
                                    <label asp-for="Assessment.Asset" class="form-label">Primary Asset</label>
                                    <input asp-for="Assessment.Asset" class="form-control" placeholder="Enter primary asset being assessed" required />
                                    <span asp-validation-for="Assessment.Asset" class="text-danger"></span>
                                </div>

                                <div class="row">
                                    <div class="col-md-6">
                                        <div class="mb-3">
                                            <label asp-for="Assessment.BusinessUnit" class="form-label">Business Unit</label>
                                            <input asp-for="Assessment.BusinessUnit" class="form-control" placeholder="Enter business unit" />
                                            <span asp-validation-for="Assessment.BusinessUnit" class="text-danger"></span>
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="mb-3">
                                            <label asp-for="Assessment.BusinessOwner" class="form-label">Business Owner</label>
                                            <input asp-for="Assessment.BusinessOwner" class="form-control" placeholder="Enter business owner" />
                                            <span asp-validation-for="Assessment.BusinessOwner" class="text-danger"></span>
                                        </div>
                                    </div>
                                </div>

                                <div class="mb-3">
                                    <label asp-for="Assessment.Description" class="form-label">Description</label>
                                    <textarea asp-for="Assessment.Description" class="form-control" rows="3" placeholder="Describe the assessment scope and objectives"></textarea>
                                    <span asp-validation-for="Assessment.Description" class="text-danger"></span>
                                </div>

                                <div class="mb-3">
                                    <label asp-for="Assessment.ThreatScenario" class="form-label">Threat Scenario</label>
                                    <textarea asp-for="Assessment.ThreatScenario" class="form-control" rows="3" placeholder="Describe the threat scenario being assessed"></textarea>
                                    <span asp-validation-for="Assessment.ThreatScenario" class="text-danger"></span>
                                </div>
                            </div>
                        </div>

                        <div class="card shadow-sm mt-4">
                            <div class="card-header bg-warning">
                                <h5 class="mb-0 text-dark"><i class="fas fa-calculator me-2"></i>Risk Scoring</h5>
                            </div>
                            <div class="card-body">
                                <div class="row">
                                    <div class="col-md-4">
                                        <div class="mb-3">
                                            <label asp-for="Assessment.QualitativeImpact" class="form-label">Impact Level</label>
                                            <select asp-for="Assessment.QualitativeImpact" class="form-select">
                                                <option value="">Select Impact</option>
                                                <option value="1">1 - Very Low</option>
                                                <option value="2">2 - Low</option>
                                                <option value="3">3 - Medium</option>
                                                <option value="4">4 - High</option>
                                                <option value="5">5 - Very High</option>
                                            </select>
                                            <span asp-validation-for="Assessment.QualitativeImpact" class="text-danger"></span>
                                        </div>
                                    </div>
                                    <div class="col-md-4">
                                        <div class="mb-3">
                                            <label asp-for="Assessment.QualitativeLikelihood" class="form-label">Likelihood Level</label>
                                            <select asp-for="Assessment.QualitativeLikelihood" class="form-select">
                                                <option value="">Select Likelihood</option>
                                                <option value="1">1 - Very Low</option>
                                                <option value="2">2 - Low</option>
                                                <option value="3">3 - Medium</option>
                                                <option value="4">4 - High</option>
                                                <option value="5">5 - Very High</option>
                                            </select>
                                            <span asp-validation-for="Assessment.QualitativeLikelihood" class="text-danger"></span>
                                        </div>
                                    </div>
                                    <div class="col-md-4">
                                        <div class="mb-3">
                                            <label asp-for="Assessment.QualitativeExposure" class="form-label">Exposure Level</label>
                                            <select asp-for="Assessment.QualitativeExposure" class="form-select">
                                                <option value="">Select Exposure</option>
                                                <option value="1">1 - Very Low</option>
                                                <option value="2">2 - Low</option>
                                                <option value="3">3 - Medium</option>
                                                <option value="4">4 - High</option>
                                                <option value="5">5 - Very High</option>
                                            </select>
                                            <span asp-validation-for="Assessment.QualitativeExposure" class="text-danger"></span>
                                        </div>
                                    </div>
                                </div>

                                @if (Model.Assessment.QualitativeImpact.HasValue && Model.Assessment.QualitativeLikelihood.HasValue)
                                {
                                    <div class="alert alert-info">
                                        <i class="fas fa-info-circle me-2"></i>
                                        <strong>Current Risk Score:</strong> 
                                        @(Model.Assessment.QualitativeImpact.Value * Model.Assessment.QualitativeLikelihood.Value * (Model.Assessment.QualitativeExposure ?? 1))
                                    </div>
                                }
                            </div>
                        </div>
                    </div>

                    <div class="col-md-4">
                        <div class="card shadow-sm">
                            <div class="card-header bg-info text-white">
                                <h5 class="mb-0"><i class="fas fa-shield-alt me-2"></i>Assessment Settings</h5>
                            </div>
                            <div class="card-body">
                                <div class="mb-3">
                                    <label asp-for="Assessment.Status" class="form-label">Status</label>
                                    <select asp-for="Assessment.Status" class="form-select">
                                        <option value="Draft">Draft</option>
                                        <option value="InProgress">In Progress</option>
                                        <option value="ReadyForReview">Ready for Review</option>
                                        <option value="Completed">Completed</option>
                                        <option value="Approved">Approved</option>
                                    </select>
                                    <span asp-validation-for="Assessment.Status" class="text-danger"></span>
                                </div>

                                <div class="mb-3">
                                    <label asp-for="Assessment.CIATriad" class="form-label">CIA Impact</label>
                                    <select asp-for="Assessment.CIATriad" class="form-select">
                                        <option value="">Select Impact Type</option>
                                        <option value="Confidentiality">Confidentiality</option>
                                        <option value="Integrity">Integrity</option>
                                        <option value="Availability">Availability</option>
                                        <option value="Mixed">Mixed</option>
                                    </select>
                                    <span asp-validation-for="Assessment.CIATriad" class="text-danger"></span>
                                </div>

                                @if (Model.Assessment.CreatedAt != default(DateTime))
                                {
                                    <div class="mb-3">
                                        <label class="form-label">Created</label>
                                        <div class="form-control-plaintext">
                                            @Model.Assessment.CreatedAt.ToString("yyyy-MM-dd HH:mm")
                                        </div>
                                    </div>
                                }

                                @if (Model.Assessment.UpdatedAt != default(DateTime))
                                {
                                    <div class="mb-3">
                                        <label class="form-label">Last Updated</label>
                                        <div class="form-control-plaintext">
                                            @Model.Assessment.UpdatedAt.ToString("yyyy-MM-dd HH:mm")
                                        </div>
                                    </div>
                                }
                            </div>
                        </div>

                        @if (Model.OpenRisks?.Any() == true)
                        {
                            <div class="card shadow-sm mt-3">
                                <div class="card-header bg-warning text-dark">
                                    <h5 class="mb-0"><i class="fas fa-exclamation-triangle me-2"></i>Related Risks</h5>
                                </div>
                                <div class="card-body">
                                    @foreach (var risk in Model.OpenRisks.Take(5))
                                    {
                                        <div class="d-flex align-items-center mb-2">
                                            <span class="badge bg-@(risk.RiskLevel.ToString().ToLower()) me-2">@risk.RiskLevel</span>
                                            <small>@risk.Title</small>
                                        </div>
                                    }
                                    @if (Model.OpenRisks.Count > 5)
                                    {
                                        <small class="text-muted">And @(Model.OpenRisks.Count - 5) more...</small>
                                    }
                                </div>
                            </div>
                        }
                    </div>
                </div>

                <div class="row mt-4">
                    <div class="col-12">
                        <div class="d-flex gap-2">
                            <!-- DEBUG: Form and button information -->
                            <div class="alert alert-warning w-100 mb-2">
                                <strong>🔧 DEBUG FORM INFO:</strong><br/>
                                <small>
                                    Form Action: Edit<br/>
                                    Assessment ID: @Model.Assessment.Id<br/>
                                    Button Text Should Be: "Save Changes"
                                </small>
                            </div>
                            <button type="submit" class="btn btn-success" id="editSubmitBtn">
                                <i class="fas fa-save me-1"></i>Save Changes
                            </button>
                            <a asp-action="Details" asp-route-id="@Model.Assessment.Id" class="btn btn-info">
                                <i class="fas fa-eye me-1"></i>View Details
                            </a>
                            <a asp-action="Index" class="btn btn-secondary">
                                <i class="fas fa-times me-1"></i>Cancel
                            </a>
                        </div>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    
    <script>
        console.log('🔧 DEBUG: Edit.cshtml Scripts section loaded');
        
        document.addEventListener('DOMContentLoaded', function() {
            console.log('🔧 DEBUG: DOM loaded for Edit view');
            
            const submitBtn = document.getElementById('editSubmitBtn');
            const form = document.querySelector('form[asp-action="Edit"]') || document.querySelector('form');
            
            console.log('🔧 DEBUG: Submit button found:', !!submitBtn);
            console.log('🔧 DEBUG: Submit button text:', submitBtn?.textContent.trim());
            console.log('🔧 DEBUG: Form found:', !!form);
            console.log('🔧 DEBUG: Form action:', form?.action);
            console.log('🔧 DEBUG: Form method:', form?.method);
            
            if (submitBtn) {
                submitBtn.addEventListener('click', function(e) {
                    console.log('🔧 DEBUG: Submit button clicked!');
                    console.log('🔧 DEBUG: Form is valid:', form?.checkValidity());
                    
                    // Don't prevent default - let form submit normally
                    // Just log what's happening
                    setTimeout(() => {
                        console.log('🔧 DEBUG: Form should have submitted by now');
                    }, 100);
                });
            }
            
            if (form) {
                form.addEventListener('submit', function(e) {
                    console.log('🔧 DEBUG: Form submit event triggered!');
                    console.log('🔧 DEBUG: Form data:', new FormData(form));
                });
            }
        });
    </script>
}